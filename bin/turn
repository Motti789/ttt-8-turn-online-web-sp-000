#!/usr/bin/env ruby

require_relative '../lib/turn'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)
turn(board)

move = gets.chomp
input_to_index
if index.between?(0,8)
  current_player
else
  loop do
  puts "Please enter 1-9:"
  break # Exit the Loop
end
end
